var toggleBtns = document.querySelectorAll('.test123');// Добавляем обработчик события на каждую кнопкуtoggleBtns.forEach(function(btn) {    btn.addEventListener('click', function() {        // Получаем контейнер с PDF файлом        var pdfContainer = btn.nextElementSibling;        // Если контейнер скрыт, то показываем его и меняем текст кнопки        if (pdfContainer.style.display === 'block') {            pdfContainer.style.display = 'none';        } else { // Иначе скрываем контейнер и меняем текст кнопки            pdfContainer.style.display = 'block';        }    });});